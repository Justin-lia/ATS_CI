$HTTP["host"] =~ "^prod.v6mig.v6connect.net$" {
  url.rewrite-repeat = (
    "^/cpe/v1/config(\?.*)?$" => "/get_v6mig_prov_config.php$1"
  )
}

$HTTP["host"] =~ "^prov.isp.rakuten.jp$" {
  url.rewrite-repeat = (
    "^/(\?.*)?$" => "/get_v6mig_prov_config.php$1"
  )
}
